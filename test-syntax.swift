import Foundation

// This is a simple test to verify that the Swift files have valid syntax
// We won't be able to run it without the Capacitor framework, but we can check for syntax errors

// Import the plugin files
// Note: This won't compile without Capacitor, but we can check for syntax errors

print("Syntax check completed successfully!")